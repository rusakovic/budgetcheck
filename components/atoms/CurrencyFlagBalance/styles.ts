import styled from '@constants/styled';
import {StyleSheet} from 'react-native';

export const CurrencyFlagBalanceStyles = StyleSheet.create({
  flag: {
    color: styled.colors.black,
  },
  balance: {
    marginLeft: '5%',
  },
});
